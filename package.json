{
  "name": "@inggl/root",
  "private": true,
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.1"
  },
  "scripts": {
    "clean": "yarn workspaces foreach -vpi run clean",
    "build": "yarn workspaces foreach -vt run build",
    "start": "yarn workspaces foreach -vpi run start",
    "watch": "yarn workspaces foreach -vpi run watch",
    "dev": "yarn workspaces foreach -vpi run dev",
    "postinstall": "husky install",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.1"
}
