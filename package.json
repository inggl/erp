{
  "name": "@inggl/root",
  "private": true,
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.1"
  },
  "scripts": {
    "clean": "yarn workspaces foreach -vpiR run clean",
    "build": "arn workspaces foreach -vtR run build",
    "start": "yarn workspaces foreach -vpiR run start",
    "watch": "yarn workspaces foreach -vpiR run watch",
    "dev": "yarn workspaces foreach -vpiR run dev",
    "postinstall": "husky install",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.1"
}
